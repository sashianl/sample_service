module-name:
    SampleService

module-description:
    Handles creating, updating, retriving samples and linking data to samples.

service-language:
    python

module-version:
    0.1.0-alpha5

owners:
    [gaprice]

service-config:
    dynamic-service: true