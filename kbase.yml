module-name:
    SampleService

module-description:
    Handles creating, updating, retriving samples and linking data to samples.

service-language:
    python

module-version:
    0.1.1

owners:
    [gaprice, slebras]

service-config:
    dynamic-service: true
